# Research

- Repo-local worklog state is tracked through AgilePlus.
- `docs/WORKLOG.md` describes the active docs lane and the historical kitty-specs migration lane.
- A canonical session bundle is needed so later sweeps can resume from `docs/sessions` rather than only the root worklog files.
